<!-- Sa2Knight: 
画面デザインろくに決まってないから、カラムわけとかしなくていいよ 
song_detailみたいにテーブルタグ直打ちだけで問題ない。そもそもカラムわけをそれぞれの
ビューに度々書いてたらフレームワークの意味無いじゃん
-->

<div class="container-fluid">
	<div class="row">
		<div class="col-xs-3 bg-info">
			<h3>hogeさんの友達</h3>
				<%= '友達' * 100 %>
		</div>
		<div class="col-xs-6 bg-success">
			<h4>最近のカラオケ</h4>
			<table class="table table-hover linkcursor">
				<tbody>
					<% @karaoke_list.each do |karaoke| %>
						<tr onclick="location.href='/karaoke/detail/<%= karaoke['id'] %>'">
							<td><%= karaoke['datetime'] %></td>
							<td><%= karaoke['name'] %></td>
						</tr>
					<% end %>
					</tr>
				</tbody>
			</table>

			<h4>最近歌った曲</h4>
			<table class="table table-hover linkcursor">
				<tbody>
					<% @histories.each do |history| %>
						<!-- Sa2Knight: 曲名には曲のリンク、歌手名には歌手のリンクのほうがよさ気 -->
						<tr onclick="location.href='/song/<%= history['song'] %>'">
							<td><%= history['song_name'] %></td>
							<td><%= history['artist_name'] %></td>
						</tr>
					<% end %>
					</tr>
				</tbody>
			</table>
			<!-- Sa2Knight: 歌手名を併記したり、何回歌ったかが書いてたらいいかも -->
			<h4>一番歌っている曲</h4>
			<%= song_link @most_sang['song']['id'], @most_sang['song']['name'] %>

			<!-- SaKnight: 何回歌われたかが欲しい -->
			<h4>一番歌っている歌手</h4>
			<%= artist_link @most_sang['artist']['id'], @most_sang['artist']['name'] %>

			<!-- Sa2Knight: 何の曲だよ -->
			<h4>最高点数</h4>
			<%= @max_score %>
		</div>
	</div>
</div>
<div class="footer">
</div>

